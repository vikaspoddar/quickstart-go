package common

import "fmt"

func GeneralDemo() {
	fmt.Println("### General demo ###")
	fmt.Println("constants are need to be declared and initialized at compile time")
	fmt.Println("syntax: const name = value (no other syntax there for constants)")
}
